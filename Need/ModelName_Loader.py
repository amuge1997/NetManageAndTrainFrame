


class Loader:
    def __init__(self):
        import os
        # Loader 所在目录
        self.sr_now_dir = os.path.split(os.path.split(__file__)[0])[1] + '/'

    def get_loader(self):
        loader = None

        return loader























